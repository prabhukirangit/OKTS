"""Layer 1½ — enrichment: fatten each concept body with synonyms/gotchas.

Implements the ``Enricher`` protocol. Must provide a deterministic offline path
so bundles build in CI without network/keys. Populated by Phase 1A.
"""
