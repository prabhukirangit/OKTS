"""Eval harness: flat BM25 baseline vs graph-aware retriever.

Reports BOTH token cost and tool-selection accuracy on the labeled query set in
``eval/queries.yaml``. No retrieval change merges without numbers from here.
Populated by Phase 1D.
"""
